/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: "/finalyearproject",
  experimental: { images: { unoptimized: true } }, basePath: "/finalyearproject", output: 'export',
  images: {unoptimized: true},experimental: {images: {unoptimized: true}},basePath: "/finalyearproject",output: 'export'
}

module.exports = nextConfig
